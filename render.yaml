services:
  - type: web
    name: eventTicketManagementSystem
    env: python
    runtime: python-3.11.9
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
